# =============================================================================
# Clipset Go Backend - Production Configuration for clipset.7ito.com
# =============================================================================
# 
# Setup:
#   1. Copy this file to .env
#   2. Generate secrets (commands below)
#   3. Set your admin password
#
# Generate secrets:
#   JWT_SECRET:         openssl rand -base64 32
#   HLS_SIGNING_SECRET: openssl rand -base64 24
#   POSTGRES_PASSWORD:  openssl rand -base64 16
#
# Start production:
#   docker compose -f docker-compose.prod.yml -f docker-compose.gpu.yml up -d
# =============================================================================

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
POSTGRES_USER=clipset
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_16
POSTGRES_DB=clipset

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_32

# JWT token expiry in hours (720 = 30 days)
JWT_EXPIRY_HOURS=720

# -----------------------------------------------------------------------------
# Initial Admin User (only used on first startup if no users exist)
# -----------------------------------------------------------------------------
INITIAL_ADMIN_EMAIL=admin@7ito.com
INITIAL_ADMIN_USERNAME=admin
INITIAL_ADMIN_PASSWORD=CHANGE_ME_YOUR_ADMIN_PASSWORD

# -----------------------------------------------------------------------------
# Server Settings
# -----------------------------------------------------------------------------
PORT=8000
HOST=0.0.0.0
ENVIRONMENT=production

# -----------------------------------------------------------------------------
# HLS Streaming
# -----------------------------------------------------------------------------
# Generate with: openssl rand -base64 24
HLS_SIGNING_SECRET=CHANGE_ME_GENERATE_WITH_openssl_rand_base64_24

# -----------------------------------------------------------------------------
# CORS Settings - Include your Cloudflare domain
# -----------------------------------------------------------------------------
CORS_ORIGINS=http://localhost,https://clipset.7ito.com

# -----------------------------------------------------------------------------
# Frontend URL (for invitation links in emails)
# -----------------------------------------------------------------------------
FRONTEND_BASE_URL=https://clipset.7ito.com

# -----------------------------------------------------------------------------
# HTTP Timeouts (for large video uploads/streaming)
# -----------------------------------------------------------------------------
HTTP_READ_TIMEOUT=10m
HTTP_WRITE_TIMEOUT=5m
